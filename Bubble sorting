#Bubble sorting
arr=[7,1,5,-15,-1,85]
b=0
arr1=[]
for i in range(0,len(arr)):
    for j in range(i+1,len(arr)):
        if arr[i]>arr[j]:
            b=arr[i]
            arr[i]=arr[j]
            arr[j]=b
    arr1.append(arr[i])
    print(i,end=" ")
print("\n")
print(arr1)
